export const enum CartFieldsConstants {
  INITIAL_SEARCH_VALUE = 3,
  ORDER_NOTES_MAX_LENGTH = 140,
}

export const enum CartFieldsPlaceholders {
  ORDER_NOTES = "Need a specific delivery day? Sending a gitf? Let’s say ...",
}
